/**
 * 一个类，必须有 `constructor`，如果没有显式定义，会默认加上 `constructor(){}`
 */

class Person {
  constructor() {
    return this; // 可以省略不写，默认返回this
  }
}

/**
 * this是 `实例对象`
 */

/**
 * constructor默认反回 `实例对象this`
 */

/**
 * constructor用途
 * `创建` 和 `初始化` class对象的特殊方法
 * 通过 `new` 生成实例对象时，自动调用constructor
 */